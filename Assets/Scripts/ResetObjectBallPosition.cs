using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResetObjectBallPosition : MonoBehaviour
{

    string[] objectBallColours = { "Yellow", "Green", "Brown", "Blue", "Pink", "Black"};

    // Awake is called when the scene first starts. Spawn balls in correct locations
    // when scene is activated. Empty game objects have been added to the table object
    // in the right locations for all coloured balls except reds.
    private void Awake()
    {



    }
    
}
